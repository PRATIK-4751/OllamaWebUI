services:
  ollama-webui:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ollama-webui
    ports:
      - "3000:80"
    restart: always
    depends_on:
      - backend
    networks:
      - ollama-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: ollama-backend
    ports:
      - "8000:8000"
    restart: always
    networks:
      - ollama-network

networks:
  ollama-network:
    driver: bridge
